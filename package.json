{
  "name": "sharqatna-mobile-suite",
  "version": "6.0.0",
  "private": true,
  "description": "شرقاطنا - نسختا المستخدم والإدارة لنظام Android",
  "scripts": {
    "prepare:apps": "node scripts/prepare-apps.mjs",
    "android:add:user": "cd apps/user && npx cap add android",
    "android:add:admin": "cd apps/admin && npx cap add android",
    "android:sync": "npm run prepare:apps && cd apps/user && npx cap sync android && cd ../admin && npx cap sync android",
    "build:user": "cd apps/user/android && ./gradlew assembleDebug",
    "build:admin": "cd apps/admin/android && ./gradlew assembleDebug",
    "build:apk": "npm run android:sync && npm run build:user && npm run build:admin"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.1",
    "@capacitor/haptics": "^6.0.2",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2",
    "onesignal-cordova-plugin": "^5.2.9"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.1",
    "sharp": "^0.33.5"
  }
}
